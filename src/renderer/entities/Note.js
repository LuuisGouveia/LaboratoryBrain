export class Note {
  constructor(client_id, client_name, output_list, period, date, total) {
    this.client_id = client_id;
    this.client_name = client_name;
    this.output_list = output_list;
    this.period = period;
    this.date = date;
    this.total = total;
  }
}
